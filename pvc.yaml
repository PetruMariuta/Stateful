apiVersion: apps/v1
kind: PersistentVolumeClaim
    - metadata:
        name: stateful5-volume
      spec:
        volumeName: stateful5-volume
        accessModes:
          - ReadWriteMany
        resources:
          requests:
            storage: 1Gi

